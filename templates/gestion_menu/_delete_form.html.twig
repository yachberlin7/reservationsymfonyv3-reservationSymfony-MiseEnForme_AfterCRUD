<form method="post" action="{{ path('app_gestion_menu_delete', {'id': repas_menu.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ repas_menu.id) }}">
    <button class="btn btn-outline-danger">Delete</button>
</form>
